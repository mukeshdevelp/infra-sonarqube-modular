[defaults]
transport = ssh

[ssh_connection]
# Use OpenSSH and pass ProxyJump and key options so dynamic inventory hosts are reachable via bastion
ssh_args = -o ProxyJump=ubuntu@3.215.184.43 -o IdentitiesOnly=yes -o StrictHostKeyChecking=no
